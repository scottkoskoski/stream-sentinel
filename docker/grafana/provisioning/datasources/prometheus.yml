# Grafana datasource provisioning -- automatically connects to the
# Prometheus instance running inside the monitoring compose stack.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
